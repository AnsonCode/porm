# Write your query or mutation here

query Test4($tak: Int!) {
  findManyPost(where: { id: { equals: "1" } }, take: $tak, skip: 0) {
    id
    updatedAt
    title
    author {
      desc
      id
    }
  }
}
